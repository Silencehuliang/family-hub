{
  "name": "family-hub",
  "version": "0.1.0",
  "private": true,
  "description": "家庭管家 - PWA 家庭管理工具集(账单/待办/购物/日程),部署于 Cloudflare",
  "license": "MIT",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm -r --parallel --stream run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "db:migrate:local": "pnpm --filter @family-hub/api exec wrangler d1 migrations apply family-hub --local",
    "db:migrate:remote": "pnpm --filter @family-hub/api exec wrangler d1 migrations apply family-hub --remote",
    "dev:api": "pnpm --filter @family-hub/api dev",
    "dev:web": "pnpm --filter @family-hub/web dev",
    "dev:pages": "pnpm --filter @family-hub/web exec wrangler pages dev dist --d1 DB=family-hub --kv KV=3686d84ff19d4e788a126e4b925e7216",
    "build:web": "pnpm --filter @family-hub/web build",
    "cf:pipeline": "pnpm --filter @family-hub/shared run typecheck && pnpm --filter @family-hub/api run lint && pnpm --filter @family-hub/web run lint && pnpm --filter @family-hub/api exec wrangler d1 migrations apply family-hub --remote && pnpm --filter @family-hub/web run build && pnpm --filter @family-hub/api exec wrangler deploy",
    "clean": "pnpm -r exec rimraf dist .wrangler node_modules"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  }
}
